
### Requirements
-[ ] use `jest` and `supertest` to write the tests.
-[ ] Write the tests BEFORE writing the route handlers..
-[ ] Your API must be have `POST` and `GET` endpoints for `/games`.
-[ ] Write a minimum of two tests per endpoint.

### Stretch Problem
-[ ] validate that the game title is unique
-[ ] add an id property to the game schema and write code in the server to increment it automatically.