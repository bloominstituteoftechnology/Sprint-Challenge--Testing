const request = require('supertest');
const server = require('./server');

describe('testing server.js', () => {
    it('testing GET /games', () => {

    })

    it('testing POST /games', () => {
        
    })

    it('testing GET  /games/:id', () => {
        
    })

    it('testing DELETE  /games/:id', () => {
        
    })
})