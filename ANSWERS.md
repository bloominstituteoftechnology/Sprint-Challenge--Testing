<!-- Answers to the Short Answer Essay Questions go here -->


1. In Jest, what are the differences between beforeAll, afterAll, beforeEach, and afterEach? When do they run? What are they used for?

These that of function methods are in all testing frameworks. 

BeforeAll is used do to something before running any of the test.

BeforeEach is used to do something before running each unit test.

AfterEach is used do to something after run each unit test.

AfterAll is to do something after all unites have ran


1. What is the point of Test Driven Development? What do you personally think about this approach?

I like DDD more than TDD. Both will help the developer to create make code and systems.

1. What is a mock function? How do we use it to test a callback passed to a function?

Most of the time mock function are used when you do not have access to a real system for testing 

1. Mention three types of tests.
1. What type of test performs database operations against a real server.

This is integration testing