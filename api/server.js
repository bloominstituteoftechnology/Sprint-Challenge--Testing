
//GET (200 status) (always return an array, even if no games)
//GET /:id (404 if doesn't exist)
//POST - unique title (405 error if not)
//DELETE /:id, (404 if doesn't exist)

module.exports = server;